var REGEXP_ALPHA = //,
  isStringWrap = require('./isString');

/**
 * Checks if value contains only letters
 *
 * @param value {string}
 */
var isAlpha = function(value) {

};

module.exports = isAlpha;